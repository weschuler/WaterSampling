1. Cloning a repository on your computer:
git clone (url of the repository)

2. To check status of git:
git status

3. Adding a file to the git:
git add (file names)

4. To commit the file to the git:
git commit -m "(committed message)" filename

5. To push the new git to the online repository:
git push -u origin main


